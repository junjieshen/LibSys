<p>
  <strong>Name:</strong>
  <%= @admin.name %>
</p>

<p>
  <strong>Email:</strong>
  <%= @admin.email %>
</p>

<% if @admin.id == current_user.id && @admin.preadmin != true %>
  <%= link_to 'Edit', edit_admin_path(@admin) %> | 
<% end %>

<%= link_to 'Back', admins_path %>
